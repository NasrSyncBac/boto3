{
  "name": "newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
   "directories": {
	"test": "tests"
   },
   "scripts": {
      "api-test-production": "newman run tests/trello.json -e env.json --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "Nasr Mohammed",
  "dependencies": {
    "newman": "^3.9.3"
  }
}
